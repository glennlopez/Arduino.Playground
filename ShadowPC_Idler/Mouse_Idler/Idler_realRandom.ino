/* Idler Type:
    - Moves the mouse randomly using environment noise to avoid smarter bot detection scripts
    - Uses environmental noise to create a true random seed to move the mouse randomly
      - Random length in xyz (not direction)
      - Random speed
      - Random direction
*/
